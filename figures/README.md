# Figures

`make_figures.py` regenerates every figure from the CSVs in `results/`, so no model call
or GPU is needed:

```bash
python -m figures.make_figures
```

Generated files are written here as PNG and PDF.
